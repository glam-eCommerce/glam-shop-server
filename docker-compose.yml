version: '3'

services:
  server:
    container_name: glam-server
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./public/uploads:/app/public/uploads
    ports:
      - 80:80
    env_file:
      - .env
    restart: always
